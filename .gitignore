# Entornos virtuales
.venv/
venv/
env/

# Python
__pycache__/
*.pyc
*.pyo

# Logs (se generan localmente, no tiene sentido subirlos)
logs/*.log

# Token cache de Outlook (contiene tokens privados)
data/token_cache.json

# Configuración con secretos (emails, contraseñas, tokens)
# Usa config.example.yaml como plantilla
config.yaml

# macOS
.DS_Store

# VS Code
.vscode/
